title: Chassis

markdown: kramdown
highlighter: rouge

baseurl: /chassis.io
permalink: pretty

sass:
  sass_dir: styles
  style: compressed

defaults:
  -
    scope:
      path: "" # empty string for all files
      type: pages
    values:
      layout: default
